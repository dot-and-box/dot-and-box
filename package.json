{
  "name": "dotsjustdots",
  "private": true,
  "version": "0.0.1",
  "description": "dots just dots is markdown for animations experiment",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.3.9",
    "vitest": "^0.34.4"
  }
}
