{
  "name": "dotsjustdots",
  "private": true,
  "version": "0.0.1",
  "description": "dots and boxes is markdown for animations experiment",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build-demo": "tsc && vite build && cp ./dist/assets/index*.js ./demos/lib/dots-and-boxes.js",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^5.0.12",
    "vitest": "^0.34.4"
  }
}
