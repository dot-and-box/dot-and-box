export enum TokenType {
    IDENTIFIER = "IDENTIFIER",
    NUMBER = "NUMBER",
    MINUS = "-",
    COMMA = ",",
    COLON = ":",
    EQUALS = "=",
    LEFT_BRACKET = "(",
    RIGHT_BRACKET = ")",
    LESS_THAN = "<",
    GREATER_THAN = ">",
    SWAP = "<->",
    MOVE_TO = "->",

}