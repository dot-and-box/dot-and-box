export enum TokenType {
    MINUS = "-",
    COMMA = ",",
    COLON = ":",
    EQUALS = "=",
    LEFT_BRACKET = "(",
    RIGHT_BRACKET = ")",
    LESS_THAN = "<",
    GREATER_THAN = ">",
    SWAP = "<->",
    MOVE_TO = "->",
    // literals
    IDENTIFIER = "IDENTIFIER",
    NUMBER = "NUMBER",
    // keywords
    TITLE = "title",
    DOTS = "dots",
    AT = "at",
    SIZE = "size",
    DATA = "data",
    BOX = "box",
    ACTIONS = "actions"
}